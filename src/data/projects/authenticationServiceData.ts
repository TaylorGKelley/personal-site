import Project from '../../types/Project';

const authenticationServiceData: Project = {
  title: 'Authentication Service API',
  content: [
    {
      heading: '',
      body: [''],
    },
  ],
} as const;

export default authenticationServiceData;
